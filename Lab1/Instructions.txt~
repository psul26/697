The file for part A is called hello.py
The file for part B is called  CNFGen.py
The language I chose to use was python

Instructions for part A:

To run part A make sure your current directory is the Lab1 folder that contains the program, bench, and state files. Then type, “python hello.py benchfile.bench statefile.state” into the terminal.
For example running ex2 looks like this,

python hello.py  ex2.bench ex2.state


Instructions for part B:

To run part A make sure your current directory is the Lab1 folder that contains the program, bench, and state files. Then type, “python CNFGen.py ex4.bench ex4.state 10” if you wanted to test ex4 with 10 unrolls. It should look like this
